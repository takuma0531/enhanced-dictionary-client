import React from "react";
import styled from "styled-components";

export default function SignIn() {
  return <SignInContainer className="signIn">
    </SignInContainer>;
}

const SignInContainer = styled.div``;
